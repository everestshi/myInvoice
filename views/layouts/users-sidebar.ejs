<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles/main.css"/>
</head>
<body>
  <%- include('../partials/header.ejs') %>
  <div class="container">
    <main id="sidebar-main"><%- body %></main>
    <section class="sidebar users-sidebar">
      <% if (users.length) { %>
        <h3>Other Users</h3>
        <ul>
          <% for (let i = 0; i < users.length; i++) { %>
            <%# skip current user %> 
            <% if (userId !== users[i].id) { %>
              <li>
                <a href="/user/users/<%= users[i].id %>"><%= users[i].username%></a>
              </li>
            <% } %>
          <% } %>
        </ul>
      <% } %>
    </section>
  </div>
  <%- include('../partials/footer.ejs') %>
</body>
</html>

