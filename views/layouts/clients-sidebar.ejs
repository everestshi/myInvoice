<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles/main.css" />
  </head>
  <body>
    <%- include('../partials/header.ejs') %>
    <main><%- body %></main>
    <section class="clients-sidebar">
      <% if (clients.length) { %>
      <h3>Other Clients</h3>
      <ul>
        <% for (let i = 0; i < clients.length; i++) {%> <%# skip current
        client %> <% if (clientId !== clients[i].id) { %>
        <li>
          <a href="/clients/<%= clients[i].id %>"><%= clients[i].name%></a>
        </li>
        <% }} %>
      </ul>
      <% } %>
    </section>
    <%- include('../partials/footer.ejs') %>
  </body>
</html>
